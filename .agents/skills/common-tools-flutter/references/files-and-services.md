<!-- Generated by `dart run tool/generate_catalog.dart`. -->
# Files and services

Use only the public imports shown below. Search exact APIs with `scripts/query_catalog.dart`.

## Contents

- [Biometric](#biometric)
- [File Service](#file-service)
- [Image Converter](#image-converter)
- [Ip Address](#ip-address)
- [Jwt Decoder](#jwt-decoder)

## Biometric

### Purpose

This family contains 1 public APIs defined by the `lib/src/services/biometric.dart` source family. No source-level purpose prose is available.

### Setup and platform

No conditional platform restriction is declared for this family under `lib/`.

### Minimal supported import

Preferred imports:
- `package:common_tools/services.dart`

```dart
import 'package:common_tools/services.dart';
```

### State, callbacks, and async

- No public controller type is declared in this family.
- No callback-bearing public type was detected.
- Async-bearing types: `Biometric`.

### Common mistakes

- Do not import the defining `lib/src/` file.
- Do not infer parameters or defaults beyond the signatures below.

### Related public symbols

`Biometric`

### `Biometric`

- Kind: class
- Import: `package:common_tools/services.dart`
- Source: `lib/src/services/biometric.dart`
- Signature: `class Biometric`

Owned public members:
- `static Future<bool> authenticate ({String? message = 'Please Authenticate to login automatically', bool biometricOnly = false})`
- `static Future<bool> get isSupported`

## File Service

### Purpose

This family contains 1 public APIs defined by the `lib/src/services/file_service.dart` source family. No source-level purpose prose is available.

### Setup and platform

No conditional platform restriction is declared for this family under `lib/`.

### Minimal supported import

Preferred imports:
- `package:common_tools/services.dart`

```dart
import 'package:common_tools/services.dart';
```

### State, callbacks, and async

- No public controller type is declared in this family.
- No callback-bearing public type was detected.
- Async-bearing types: `FileService`.

### Common mistakes

- Do not import the defining `lib/src/` file.
- Do not infer parameters or defaults beyond the signatures below.

### Related public symbols

`FileService`

### `FileService`

- Kind: class
- Import: `package:common_tools/services.dart`
- Source: `lib/src/services/file_service.dart`
- Signature: `class FileService`

Owned public members:
- `static Future<List<MyPickedFile>?> pickFiles ({List<String>? allowedExtensions, int? maxSizeInBytes})`
- `static Future<List<MyPickedFile>?> pickMultipleImages ({double? maxWidth, double? maxHeight, int? quality, int? maxLimit})`
- `static Future<List<MyPickedFile>?> pickMultipleVideos ({Duration? maxDuration, int? maxSizeInBytes, int? maxLimit})`
- `static Future<MyPickedFile?> pickFile ({List<String>? allowedExtensions, int? maxSizeInBytes})`
- `static Future<MyPickedFile?> pickImage ({ImageSource imageSource = ImageSource.gallery, double? maxWidth, double? maxHeight, int? quality, int? maxSizeInBytes})`
- `static Future<MyPickedFile?> pickVideo ({int? maxSizeInBytes, Duration? maxDuration, ImageSource source = ImageSource.gallery})`
- `static Future<MyPickedFile?> showFilePickerPopup (BuildContext context, {bool isFilePicker = false, List<String>? allowedExtensions})`

## Image Converter

### Purpose

This family contains 1 public APIs defined by the `lib/src/services/image_converter.dart` source family. No source-level purpose prose is available.

### Setup and platform

No conditional platform restriction is declared for this family under `lib/`.

### Minimal supported import

Preferred imports:
- `package:common_tools/services.dart`

```dart
import 'package:common_tools/services.dart';
```

### State, callbacks, and async

- No public controller type is declared in this family.
- No callback-bearing public type was detected.
- Async-bearing types: `ImageConverter`.

### Common mistakes

- Do not import the defining `lib/src/` file.
- Do not infer parameters or defaults beyond the signatures below.

### Related public symbols

`ImageConverter`

### `ImageConverter`

- Kind: class
- Import: `package:common_tools/services.dart`
- Source: `lib/src/services/image_converter.dart`
- Signature: `class ImageConverter`

Owned public members:
- `static Future<String> toBase64String (Object object)`
- `static Future<Uint8List> getFromSvgAsset (String assetName, {Size size = const Size(48, 48), BuildContext? context, ColorMapper? colorMapper})`
- `static Future<Uint8List> getFromWidget (Widget widget, {Duration delay = const Duration(milliseconds: 100), BuildContext? context, Size logicalSize = const Size(800, 600)})`
- `static Uint8List fromBase64String (String base64String)`
- `static bool get isCupertino`

## Ip Address

### Purpose

Exception thrown when an IP address cannot be resolved as a string.

### Setup and platform

No conditional platform restriction is declared for this family under `lib/`.

### Minimal supported import

Preferred imports:
- `package:common_tools/services.dart`

```dart
import 'package:common_tools/services.dart';
```

### State, callbacks, and async

- No public controller type is declared in this family.
- No callback-bearing public type was detected.
- Async-bearing types: `IPAddressService`, `getIPAddress`.

### Common mistakes

- Do not import the defining `lib/src/` file.
- Do not infer parameters or defaults beyond the signatures below.

### Related public symbols

`IPAddressException`, `IPAddressFormat`, `IPAddressService`, `IPAddressVersion`, `getIPAddress`, `handleJSONResponse`

### `IPAddressException`

- Kind: class
- Import: `package:common_tools/services.dart`
- Source: `lib/src/services/ip_address.dart`
- Signature: `class IPAddressException implements Exception`

Exception thrown when an IP address cannot be resolved as a string.

Owned public members:
- `String toString ()`
- `const IPAddressException (this.message, {this.cause})`
- `final Object? cause;`
- `final String message;`

### `IPAddressFormat`

- Kind: enum
- Import: `package:common_tools/services.dart`
- Source: `lib/src/services/ip_address.dart`
- Signature: `enum IPAddressFormat`

Enum representing the format in which the IP address should be returned.

Owned public members:
- `json`
- `string`

### `IPAddressService`

- Kind: class
- Import: `package:common_tools/services.dart`
- Source: `lib/src/services/ip_address.dart`
- Signature: `class IPAddressService`

Retrieves the public IP address using the package network layer.

Owned public members:
- `Future<Object> getIPAddress ({IPAddressFormat ipAddressFormat = IPAddressFormat.string, String defaultErrorMessage = 'Not able to find the IP Address.', IPAddressVersion ipAddressVersion = IPAddressVersion.v64})`
- `IPAddressService ({NetworkClient? networkClient})`

### `IPAddressVersion`

- Kind: enum
- Import: `package:common_tools/services.dart`
- Source: `lib/src/services/ip_address.dart`
- Signature: `enum IPAddressVersion`

IP protocol endpoint to use when resolving the public address.

Owned public members:
- `v4`
- `v6`
- `v64`

### `getIPAddress`

- Kind: function
- Import: `package:common_tools/services.dart`
- Source: `lib/src/services/ip_address.dart`
- Signature: `Future<Object> getIPAddress({IPAddressFormat ipAddressFormat = IPAddressFormat.string, String defaultErrorMessage = 'Not able to find the IP Address.', IPAddressVersion ipAddressVersion = IPAddressVersion.v64, NetworkClient? networkClient})`

Retrieves the public IP address using the package network layer.

This top-level helper preserves the previous API while allowing callers to
inject a custom [networkClient] for shared configuration or tests.

### `handleJSONResponse`

- Kind: function
- Import: `package:common_tools/services.dart`
- Source: `lib/src/services/ip_address.dart`
- Signature: `Map<String, Object> handleJSONResponse({bool status = false, String value = ''})`

Handles the JSON response for IP address retrieval.

Returns a map with `status` and `ip_address` keys.

## Jwt Decoder

### Purpose

This family contains 1 public APIs defined by the `lib/src/services/jwt_decoder.dart` source family. No source-level purpose prose is available.

### Setup and platform

No conditional platform restriction is declared for this family under `lib/`.

### Minimal supported import

Preferred imports:
- `package:common_tools/services.dart`

```dart
import 'package:common_tools/services.dart';
```

### State, callbacks, and async

- No public controller type is declared in this family.
- No callback-bearing public type was detected.
- No public `Future` or `Stream` signature was detected.

### Common mistakes

- Do not import the defining `lib/src/` file.
- Do not infer parameters or defaults beyond the signatures below.

### Related public symbols

`JwtDecoder`

### `JwtDecoder`

- Kind: class
- Import: `package:common_tools/services.dart`
- Source: `lib/src/services/jwt_decoder.dart`
- Signature: `class JwtDecoder`

Owned public members:
- `static DateTime getExpirationDate (String token)`
- `static Duration getRemainingTime (String token)`
- `static Duration getTokenTime (String token)`
- `static Map<String, dynamic>? decode (String token)`
- `static Map<String, dynamic>? tryDecode (String token)`
- `static bool isExpired (String token)`

